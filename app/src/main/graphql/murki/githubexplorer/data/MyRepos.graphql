query MyRepos($count: Int!, $after: String) {
  viewer {
    repositories(first: $count, after:$after) {
      repositoryEdges: edges {
        cursor
        repositoryNode: node {
          id
          name
          description
        }
      }
    }
  }
}
